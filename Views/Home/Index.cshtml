@{
    ViewData["Title"] = "Home Page";
}
<!DOCTYPE html>
<html>
<style>
    #mydiv {
        position: absolute;
        z-index: 9;
        background-color: #f1f1f1;
        text-align: center;
        border: 1px solid #d3d3d3;
        width: 20%;
        cursor: move;
    }

    #mydivheader {
        padding: 10px;
        cursor: move;
        z-index: 10;
        background-color: #2196F3;
        color: #fff;
    }

    .vl {
        border-left: 6px solid green;
        height: 500px;
        position: absolute;
        left: 20%;
        margin-left: -3px;
        top: 0;
    }

    .v2 {
        border-left: 6px solid green;
        height: 500px;
        position: absolute;
        left: 40%;
        margin-left: -3px;
        top: 0;
    }

    .v3 {
        border-left: 6px solid green;
        height: 500px;
        position: absolute;
        left: 60%;
        margin-left: -3px;
        top: 0;
    }

    .v4 {
        border-left: 6px solid green;
        height: 500px;
        position: absolute;
        left: 80%;
        margin-left: -3px;
        top: 0;
    }
</style>
<body>
    <div class="vl"></div>
    <div class="v2"></div>
    <div class="v3"></div>
    <div class="v4"></div>

    <div id="mydiv">
        <p>Jan Kowalski</p>
        <p>Jakieś add info</p>
    </div>

    <script>
        //Make the DIV element draggagle:
        dragElement(document.getElementById("mydiv"));

        function dragElement(elmnt) {
            var pos1 = 0, pos2 = 0, pos3 = 0, pos4 = 0;
            if (document.getElementById(elmnt.id + "header")) {
                /* if present, the header is where you move the DIV from:*/
                document.getElementById(elmnt.id + "header").onmousedown = dragMouseDown;
            } else {
                /* otherwise, move the DIV from anywhere inside the DIV:*/
                elmnt.onmousedown = dragMouseDown;
            }

            function dragMouseDown(e) {
                e = e || window.event;
                e.preventDefault();
                // get the mouse cursor position at startup:
                pos3 = e.clientX;
                pos4 = e.clientY;
                document.onmouseup = closeDragElement;
                // call a function whenever the cursor moves:
                document.onmousemove = elementDrag;
            }

            function elementDrag(e) {
                e = e || window.event;
                e.preventDefault();
                // calculate the new cursor position:
                pos1 = pos3 - e.clientX;
                pos2 = pos4 - e.clientY;
                pos3 = e.clientX;
                pos4 = e.clientY;
                // set the element's new position:
                elmnt.style.top = (elmnt.offsetTop - pos2) + "px";
                //if
                elmnt.style.left = (elmnt.offsetLeft - pos1) + "px";
            }

            function closeDragElement(e) {
                /* stop moving when mouse button is released:*/
                e = e || window.event;
                e.preventDefault();
                pos1 = pos3 - e.clientX;
                console.log(elmnt.offsetLeft - pos1 + "    /     " + getWidth());
                if (elmnt.offsetLeft - pos1 < 0) {
                    elmnt.style.left = getWidth() * 0 + "px";
                }
                if ((elmnt.offsetLeft - pos1 >= 0) && (elmnt.offsetLeft - pos1 <= getWidth() * 0.10)){
                    elmnt.style.left = getWidth() * 0 + "px";
                }
                if ((elmnt.offsetLeft - pos1 > getWidth() * 0.10) && (elmnt.offsetLeft - pos1 <= getWidth() * 0.3)) {
                    elmnt.style.left = getWidth() * 0.2 + "px";
                }
                if ((elmnt.offsetLeft - pos1 > getWidth() * 0.3) && (elmnt.offsetLeft - pos1 <= getWidth() * 0.5)){
                    elmnt.style.left = getWidth() * 0.4 + "px";
                }
                if ((elmnt.offsetLeft - pos1 > getWidth() * 0.5) && (elmnt.offsetLeft - pos1 <= getWidth() * 0.7)) {
                    elmnt.style.left = getWidth() * 0.6 + "px";
                }
                if ((elmnt.offsetLeft - pos1 > getWidth() * 0.7) && (elmnt.offsetLeft - pos1 <= getWidth() * 1)) {
                    elmnt.style.left = getWidth * 0.8 + "px";
                }
                document.onmouseup = null;
                document.onmousemove = null;
            }

            function getWidth() {
                return Math.max(
                    document.body.scrollWidth,
                    document.documentElement.scrollWidth,
                    document.body.offsetWidth,
                    document.documentElement.offsetWidth,
                    document.documentElement.clientWidth
                );
            }

            function getHeight() {
                return Math.max(
                    document.body.scrollHeight,
                    document.documentElement.scrollHeight,
                    document.body.offsetHeight,
                    document.documentElement.offsetHeight,
                    document.documentElement.clientHeight
                );
            }
        }
    </script>

</body>
</html>
